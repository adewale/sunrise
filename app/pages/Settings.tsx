import { Link, Form } from '@ts-76/inertia-hono-jsx';

export default function Settings(props: any) {
  const pageSize = props.settings?.inboxPageSize ?? 50;
  const includeSubscribed = props.settings?.includeSubscribedNotifications === true;
  const update = props.update;
  return <><section class="section panel settings-panel"><div class="section-head"><div><p class="eyebrow">Preferences</p><h1>Settings</h1><p class="muted">Keep Sunrise quiet, skimmable, and tuned to how much GitHub you want at once.</p></div></div><Form method="post" action="/settings" class="settings-form">{({ processing }: { processing: boolean }) => <><label><span>Inbox page size</span><select name="inboxPageSize">{[25, 50, 100].map((n) => <option value={n} selected={pageSize === n}>{n} events</option>)}</select></label><p class="muted">Pagination starts after this many events. Default is 50.</p><label class="checkbox-row"><input type="checkbox" name="includeSubscribedNotifications" checked={includeSubscribed} /><span>Include watched-repository notifications</span></label><p class="muted">Off by default. Mentions, assignments, review requests, authored threads, invitations, owned-repo PRs, workflow failures, and security alerts still appear.</p><button class="button primary" type="submit" aria-busy={processing || undefined}>{processing ? 'Saving...' : 'Save settings'}</button></>}</Form></section><section class="section panel"><div class="section-head"><div><p class="eyebrow">Sunrise version</p><h2>{update?.hasUnseenChangelog ? 'What changed' : 'Up to date locally'}</h2><p class="muted">Current version {update?.currentVersion ?? props.version?.version}. No deployment telemetry is sent upstream.</p></div><Link class="button primary" href="/changelog">View changelog</Link></div><div class="config-card"><p><span>Agent upgrade contract</span><code>{props.version?.upgradeContract}</code></p><p><span>Upstream</span><code>{props.version?.upstream}</code></p></div></section></>;
}
