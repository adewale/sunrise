{
  "name": "sunrise",
  "version": "0.2.0",
  "private": true,
  "description": "Self-hosted GitHub action dashboard on Cloudflare.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "types:cf": "wrangler types",
    "verify": "npm run types:cf && npm run typecheck && npm test",
    "ship-check": "node scripts/ship-check.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite",
    "deploy": "wrangler d1 migrations apply DB --remote && vite build && wrangler deploy",
    "fixtures:capture": "node scripts/capture-github-fixtures.mjs",
    "screenshots:capture": "node scripts/capture-screenshots.mjs",
    "release": "node scripts/release.mjs"
  },
  "cloudflare": {
    "bindings": {
      "GITHUB_CLIENT_ID": {
        "description": "GitHub OAuth App Client ID. Create an OAuth App at https://github.com/settings/developers after Cloudflare shows your workers.dev URL."
      },
      "GITHUB_CLIENT_SECRET": {
        "description": "GitHub OAuth App Client Secret from the same OAuth App. Store as a secret."
      },
      "OWNER_LOGIN": {
        "description": "Your GitHub username. Sunrise only allows this single owner to sign in."
      },
      "SESSION_SECRET": {
        "description": "Random long secret used for local session hardening. Generate with `openssl rand -base64 32`."
      }
    }
  },
  "dependencies": {
    "@hono/inertia": "^0.2.0",
    "hono": "^4.12.16"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.38.0",
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "@inertiajs/core": "^3.2.0",
    "@ts-76/inertia-hono-jsx": "^0.2.3",
    "@types/node": "^25.6.0",
    "playwright": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.14",
    "vite-ssr-components": "^0.6.1",
    "vitest": "^4.1.5",
    "wrangler": "^4.87.0"
  }
}
